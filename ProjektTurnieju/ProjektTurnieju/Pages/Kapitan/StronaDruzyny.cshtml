@page
@model ProjektTurnieju.Pages.Kapitan.StronaDruzynyModel

@{
    ViewData["Title"] = "Strona Druzyny";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.druzynaKapitana.NazwaDruzyny)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Druzyny) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NazwaDruzyny)
            </td>
            <td>
                <a asp-page="/Kapitan/Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="/Kapitan/Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="/Kapitan/Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
